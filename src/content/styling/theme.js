import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#FFFFFF",
      contrastText: "#000000",
    },
    secondary: {
      main: "#e50914",
      contrastText: "#FFFFFF",
    },
    black: {
      main: "#000000",
      contrastText: "#FFFFFF",
    },
  },
});
